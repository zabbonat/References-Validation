{
  "name": "checkifexist-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for CheckIfExist — verify academic references against CrossRef, Semantic Scholar, OpenAlex, arXiv, and DBLP",
  "type": "module",
  "bin": {
    "checkifexist-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "echo \"No build required. Run directly via tsx (npm run dev)\"",
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "@types/node": "^24.12.2",
    "tsx": "^4.19.0"
  }
}
